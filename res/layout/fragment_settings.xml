<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.hubtrail.SettingsActivity$PlaceholderFragment" >

 <CheckBox android:id="@+id/checkbox_earthview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/earthview"
        android:checked="false"
        android:onClick="onCheckboxClicked"/>

  <CheckBox
      android:id="@+id/checkbox_mylocation"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_alignLeft="@+id/checkbox_earthview"
      android:layout_below="@+id/checkbox_earthview"
      android:checked="true"
      android:onClick="onCheckboxClicked"
      android:text="@string/mylocation" />

</RelativeLayout>
